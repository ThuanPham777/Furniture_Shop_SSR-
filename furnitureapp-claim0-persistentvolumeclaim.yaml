apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: furnitureapp-claim0
  name: furnitureapp-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
